from ultralytics import RTDETR

def check_model_classes():
    # Load your current 5-class best weights
    model = RTDETR('C:/Users/hsmsa/runs/detect/runs/detect/retrain_balanced/rtdetr_ppe_balanced_v2/weights/best.pt')
    
    print("Model's Internal Class Mapping:")
    for class_id, class_name in model.names.items():
        print(f"{class_id}: {class_name}")

if __name__ == '__main__':
    check_model_classes()